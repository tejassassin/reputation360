---
description: Use ASCII hyphen instead of em dash in all copy and code
globs: "**/*.{jsx,tsx,js,mjs,ts,css,md,html,json}"
alwaysApply: true
---

# Hyphens, not em dashes

Do not use the Unicode em dash (—, U+2014). Use a single ASCII hyphen-minus (-) instead for pauses, asides, and similar phrasing.

When editing existing text that contains —, replace it with `-`.
