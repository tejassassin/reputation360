/**
 * Absolute production site URL. Same role as Next.js `metadataBase`: prefix for
 * canonical links, Open Graph URLs, and sitemap generation.
 */
export const METADATA_BASE = "https://www.thereputation360.com";

/** Alias kept for call sites that name the origin this way. */
export const SITE_CANONICAL_ORIGIN = METADATA_BASE;
