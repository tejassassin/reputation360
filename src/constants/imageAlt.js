/** Nav header + mobile nav (same logo instance). */
export const LOGO_ALT_NAV =
  "Reputation360 - Online Reputation Management Services";

/** Footer logo. */
export const LOGO_ALT_FOOTER = "Reputation360 - Reputation Management Company";

export const WHAT_WE_BELIEVE_SERP_MOCKUP_ALT =
  "Before-and-after Google search results showing negative links suppressed and positive content ranking on page one after Reputation360's ORM campaign";

/** @param {string} reviewerName */
export function testimonialPortraitAlt(reviewerName) {
  return `Headshot of ${reviewerName}, Reputation360 client`;
}
